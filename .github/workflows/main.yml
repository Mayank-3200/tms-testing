name: Run Postman Collection

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  postman-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Newman + HTML Reporter
        run: |
          npm install -g newman
          npm install -g newman-reporter-htmlextra

      - name: Run Postman Collection
        run: |
          newman run application.json \
          --reporters cli,htmlextra \
          --reporter-htmlextra-export results.html \
          --reporter-htmlextra-title "CI/CD Test Report" \
          --reporter-htmlextra-titleSize 4 \
          --reporter-htmlextra-showEnvironmentData

      - name: Upload TMS Collection HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: newman-report
          path: |
            results.html
            results.json
      - name: Extract JSON summary
        id: extract
        run: |
          summary=$(jq '{total: .run.stats.tests.total, passed: .run.stats.tests.passed, failed: .run.stats.tests.failed, skipped: .run.stats.tests.skipped}' results.json)
          echo "SUMMARY=$summary" >> $GITHUB_ENV

      - name: Call REST API with summary + HTML report
        run: |
          # Extract counts from JSON
          total=$(jq '.run.stats.tests.total' results.json)
          passed=$(jq '.run.stats.tests.passed' results.json)
          failed=$(jq '.run.stats.tests.failed' results.json)
          skipped=$(jq '.run.stats.tests.skipped' results.json)

          # Call your API with file + counts
          curl --location 'http://localhost:5001/api/v1/api-test-runner/get-files' \
            --header 'ngrok-skip-browser-warning: 69420' \
            --header 'Referer: http://localhost:5173/' \
            --header 'User-Agent: GitHubActionsBot/1.0' \
            --header 'Accept: application/json, text/plain, */*' \
            -F "attachments=@results.html;type=text/html" \
            -F "total=$total" \
            -F "pass=$passed" \
            -F "failed=$failed" \
            -F "skip=$skipped"

      - name: Send email with report
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.PASSWORD }}
          subject: "Postman Test Report"
          to: ${{ secrets.TO }}
          from: ${{ secrets.FROM }}
          body: "Attached is the Postman test run report."
          attachments: results.html
