name: Run Postman Collection

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  postman-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Newman + HTML Reporter
        run: |
          npm install -g newman
          npm install -g newman-reporter-htmlextra

      - name: Run Postman Collection
        run: |
          newman run application.json \
            --reporters cli,htmlextra \
            --reporter-htmlextra-export results.html

      - name: Upload TMS Collection HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: newman-report
          path: results.html

      - name: Send email with report
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: moon.blockchaindeveloper@gmail.com
          password: xcmhuhcjgerxehan
          subject: "Postman Test Report"
          to: "mayank.s@moontechnolabs.com"
          from: "733de776f4-5d8e28@inbox.mailtrap.io"
          body: "Attached is the Postman test run report."
          attachments: results.html
